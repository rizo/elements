(jbuild_version 1)

(library
 ((name Data)
  (public_name proto.data)
  (libraries (proto.stdlib proto.base proto.control))
  (flags (:standard -open Base -open Control))
  (modules (Array
            Either
            List
            Dict
            Option
            Result
            Stream
            String
            Tuple
            Void))))

